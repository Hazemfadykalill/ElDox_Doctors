import { AllDoctorsPipe } from './all-doctors.pipe';

describe('AllDoctorsPipe', () => {
  it('create an instance', () => {
    const pipe = new AllDoctorsPipe();
    expect(pipe).toBeTruthy();
  });
});
