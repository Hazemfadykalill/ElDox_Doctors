import { CustomDayPipe } from './custom-day.pipe';

describe('CustomDayPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomDayPipe();
    expect(pipe).toBeTruthy();
  });
});
