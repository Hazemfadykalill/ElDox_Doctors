import { DayCustomPipe } from './day-custom.pipe';

describe('DayCustomPipe', () => {
  it('create an instance', () => {
    const pipe = new DayCustomPipe();
    expect(pipe).toBeTruthy();
  });
});
